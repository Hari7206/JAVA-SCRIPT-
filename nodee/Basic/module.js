
// this is how we will take our comoon js file require in old time
// const a  = require("./moduledepend")
// console.log(a);

// but for now we use the import with in es6 for that we have to make the type in the package.json as the module type after that we can import our file from the export that of two type name and the default like this here we write that code
import user from "./moduledepend.js"
console.log(user);

